version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: kids-oasis-db
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7.0-alpine
    container_name: kids-oasis-cache
    ports:
      - "6379:6379"

volumes:
  mongo-data:
